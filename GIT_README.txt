To make a new commit to an existing repository, do the following in Git Bash:
 1. git add .
 2. git commit -m "<Describe commit here>"
 3. git push

This should be done after any changes are made to the repository or its files.